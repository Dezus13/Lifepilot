# Связь экранов и данных MVP LifePilot

## Назначение документа

Этот документ связывает экраны MVP с данными: что экран читает, что создает и что изменяет. Он нужен, чтобы экранная архитектура не смешивала presentation logic, хранение и продуктовые решения.

Каноническая структура Case, `StoredCase`, `SupabaseCaseRow`, статусы и жизненный цикл описаны в [../specs/case-model.md](../specs/case-model.md). Этот документ описывает экранное использование данных и не является отдельным источником правды по Case.

## Onboarding

Использует:

- текстовые ограничения продукта;
- ссылки на главный экран и настройки безопасности.

Не создает и не изменяет данные кейса.

## Главная

Использует:

- статический список быстрых действий;
- `lifepilot.caseHistory`;
- `analysis.status`, если он сохранен;
- `status`;
- `riskLevel`;
- `analysis.riskLevel`, если он сохранен;
- `analysis.priorityLevel`, если он сохранен;
- `createdAt`, если он сохранен;
- `updatedAt`;
- preview последнего кейса, если история не пуста;
- ссылки на создание кейса, историю и настройки безопасности.

Создает:

- намерение пользователя начать новый кейс.
- dashboard-статистику: всего кейсов, требуют действия, высокий риск, в ожидании, завершено;
- блок `Последний кейс` с названием, статусом, priority и датой создания;
- empty state `История пока пуста`, если локальных кейсов нет;
- fallback `Не найдено` для старых записей без новых полей.

Не изменяет данные кейса напрямую. Не создает backend, auth, database или внешний запрос.

## Новый кейс

Использует:

- локальное состояние поля ввода;
- локальное состояние поля ввода;
- локальный результат `createLocalAnalysis()`, который определяет категорию;
- inline-warning для пустого текста.

Создает:

- новый объект текущего кейса;
- `id`;
- `sourceText`;
- `category`, рассчитанную локальным анализом;
- `riskLevel`;
- `analysis.status`;
- `status` как дубликат `analysis.status`;
- `updatedAt`.

Изменяет:

- записывает кейс в `lifepilot.currentCase`;
- добавляет кейс в `lifepilot.caseHistory`;
- очищает warning при изменении текста.

## Анализ

Использует:

- наличие `lifepilot.currentCase`;
- состояние таймера или loading-состояние;
- список шагов обработки.

Создает:

- не создает отдельный результат анализа в текущем MVP;
- показывает промежуточное состояние обработки.

Изменяет:

- не изменяет сохраненный кейс;
- переводит пользователя к результату после короткого ожидания;
- возвращает на экран нового кейса, если текущий кейс отсутствует.

## Результат

Использует:

- `lifepilot.currentCase`;
- `sourceText`;
- `category`;
- `riskLevel`;
- `status`;
- `updatedAt`;
- `analysis.extractedData`, если он уже сохранен;
- `analysis.status`, если он уже сохранен;
- `analysis.actionPlan`, сформированный Action Plan Engine;
- локальные правила отображения риска;
- резервное значение категории `Другое`.

Создает:

- краткое отображение анализа из исходного текста;
- risk badge;
- блок `Статус кейса` с `analysis.status`;
- объяснение уровня риска;
- блок `Самое важное` с `analysis.prioritySummary`, `analysis.priorityLevel`, ближайшим сроком, главным действием и главным последствием;
- fallback `Не найдено` для отсутствующих priority-значений;
- блок `Статус срока` с `analysis.deadlineStatus`, `analysis.daysRemaining`, `analysis.deadlineMessage` и датой срока;
- блок `Wichtige Fakten` с организацией, типом документа, номером дела, сроками, суммой, контактами, последствиями и уровнем риска;
- блок `План действий` с нумерованным списком 3-5 шагов из `analysis.actionPlan`;
- безопасное empty state, если кейс не найден.

Изменяет:

- в текущем MVP не записывает новый результат в хранилище;
- может пересчитать локальный анализ для отображения, если сохраненный кейс старый и не содержит новых полей важных фактов, priority-полей или `analysis.status`;
- переводит пользователя к экрану черновика через навигацию.

## Черновик

Использует:

- `lifepilot.currentCase`;
- `analysis.riskLevel` или top-level `riskLevel`;
- `analysis.category` или top-level `category`;
- демонстрационный немецкий текст, выбранный по risk level;
- предупреждение о ручной проверке.

Читает текущий кейс, но не изменяет `lifepilot.currentCase`, не изменяет `lifepilot.caseHistory` и не сохраняет текст черновика отдельным полем. В текущем MVP черновик является демонстрационным шаблоном текущего нейтрального потока.

## Риск

Использует:

- статическое предупреждение о рискованных ситуациях.

Не читает и не изменяет сохраненный кейс.

## История

Использует:

- `lifepilot.caseHistory`;
- `id`;
- `sourceText`;
- `category`;
- `riskLevel`;
- `analysis`, включая `analysis.extractedData`;
- `analysis.actionPlan`, если он сохранен;
- `analysis.prioritySummary` и `analysis.priorityLevel`, если они сохранены;
- `analysis.deadlineStatus`, `analysis.daysRemaining` и `analysis.deadlineMessage`, если они сохранены;
- `analysis.status`, если он сохранен;
- `status`;
- `updatedAt`;
- резервную категорию `Другое`;
- расчет `riskLevel` для старых записей без уровня риска.
- сохраненные priority-данные внутри `analysis` в `lifepilot.caseHistory`.
- fallback для старых русскоязычных статусов без `CaseStatus`.

Создает:

- локальный поиск по title, preview, organization, documentType и keywords;
- фильтр по статусу, где значения `CaseStatus` берутся из [../specs/case-model.md](../specs/case-model.md);
- фильтр по приоритету: все, `critical`, `high`, `medium`, `low`;
- сортировку: новые сначала, старые сначала, `critical`/`high` сначала;
- список карточек истории;
- preview текста;
- короткий заголовок кейса;
- человекочитаемую дату;
- risk badge.
- ссылку `Открыть кейс` на `/history/[caseId]`;
- действие `Открыть результат` для восстановления кейса как текущего.
- empty state `Кейсы не найдены`, если локальная фильтрация не вернула результатов;
- fallback `Не найдено` для отсутствующих значений в старых карточках истории.

Изменяет:

- не записывает поисковый запрос, фильтры или сортировку в `localStorage`;
- при действии `Открыть результат` записывает выбранный кейс в `lifepilot.currentCase`;
- при переходе на `/history/[caseId]` не обязан изменять `lifepilot.currentCase`;
- при очистке истории удаляет `lifepilot.caseHistory`;
- обновляет локальное состояние списка после очистки.

## Просмотр сохраненного кейса

Использует:

- параметр маршрута `caseId`;
- `lifepilot.caseHistory`;
- `id`;
- `sourceText`;
- `status`;
- `analysis.status`;
- `analysis.priorityLevel`;
- `analysis.prioritySummary`;
- `analysis.deadlineMessage`;
- `analysis.extractedData.organization`;
- `analysis.extractedData.documentType`;
- `analysis.extractedData.documentImportance`;
- `analysis.extractedData.requiredAction`;
- `analysis.extractedData.consequences`;
- `analysis.actionPlan`;
- `analysis.foundKeywords`.

Создает:

- detail view сохраненного кейса;
- preview названия и исходного текста;
- блок статуса кейса;
- блок приоритета и важных фактов;
- список шагов плана действий;
- список найденных ключевых слов;
- безопасное empty state, если кейс не найден;
- локальный пересчет анализа из `sourceText`, если сохраненный кейс старый и не содержит новых analysis-полей;
- fallback `Не найдено` для отдельных фактов, которые не найдены даже после пересчета.

Изменяет:

- не изменяет `lifepilot.currentCase`;
- не изменяет `lifepilot.caseHistory`;
- не создает backend, auth, database или внешний запрос.

## Безопасность

Использует:

- текстовые правила ответственности.

Не создает и не изменяет данные кейса.

## Ошибка и fallback

Использует:

- контекст ошибки, если он доступен;
- безопасные ссылки возврата.

Создает:

- сообщение о невозможности продолжить сценарий.

Изменяет:

- не должен изменять кейс без явного действия пользователя;
- не должен очищать текущий ввод или историю автоматически.

## Общие правила связи экранов и данных

- Исходный текст не должен передаваться через URL.
- Экран результата читает данные, но не считается источником сохраненного результата.
- История работает только с сохраненными локальными кейсами.
- Открытие истории восстанавливает кейс как текущий, а не создает новый.
- Просмотр сохраненного кейса читает запись по `id` и не создает скрытые данные.

## Связанные документы

- [state-management.md](./state-management.md) описывает уровни состояния.
- [data-flow.md](./data-flow.md) описывает порядок движения данных.
- [routing-map.md](./routing-map.md) описывает маршруты экранов.
- [../plans/completed/screens-list.md](../plans/completed/screens-list.md) является архивным экранным планом.
- [../plans/completed/screens-flow.md](../plans/completed/screens-flow.md) является архивным UI-flow планом.
